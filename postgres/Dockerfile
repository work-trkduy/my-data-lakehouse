# PostgreSQL 16 source DB pre-configured for logical (WAL) replication used by Debezium.
# WAL-level + sender/slot limits are passed via command line in docker-compose, so this
# image only documents intent; init SQL is bind-mounted into /docker-entrypoint-initdb.d.
FROM postgres:16